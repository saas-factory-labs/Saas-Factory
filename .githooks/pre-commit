#!/bin/sh
#!/usr/bin/env bash
# File generated by pre-commit: https://pre-commit.com
# ID: 138fd403232d2ddd5efb44317e38bf03

# start templated
INSTALL_PYTHON='C:\Users\caspe\AppData\Local\Python\pythoncore-3.14-64\python.exe'
ARGS=(hook-impl --config=.pre-commit-config.yaml --hook-type=pre-commit)
# end templated

HERE="$(cd "$(dirname "$0")" && pwd)"
ARGS+=(--hook-dir "$HERE" -- "$@")

if [ -x "$INSTALL_PYTHON" ]; then
    "$INSTALL_PYTHON" -mpre_commit "${ARGS[@]}"
    exit_code=$?
elif command -v pre-commit > /dev/null; then
    pre-commit "${ARGS[@]}"
    exit_code=$?
else
    echo '`pre-commit` not found.  Did you forget to activate your virtualenv?' 1>&2
    exit 1
fi

# Remove Windows null-device artifact created by pre-commit hooks running in Git Bash
rm -f "$(git rev-parse --show-toplevel)/nul"
exit $exit_code
