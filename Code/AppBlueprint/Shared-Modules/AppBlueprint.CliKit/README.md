# AppBlueprint.CliKit

Reusable command-line infrastructure for AppBlueprint-based tools.

The package intentionally stays small: command registration, Spectre.Console helpers, process execution, local JSON configuration, GitHub Actions output files, and runtime preflight helpers.
