<div class="flex flex-col col-span-full sm:col-span-6 bg-white dark:bg-gray-800 shadow-xs rounded-xl">
    <header class="px-5 py-4 border-b border-gray-100 dark:border-gray-700/60 flex items-center">
        <h2 class="font-semibold text-gray-800 dark:text-gray-100">Sales VS Refunds</h2>
        @* TODO: Migrate to use Tooltip component from AppBlueprint.UiKit.Components.Shared.Cruip *@
        @* <Tooltip Size="lg">
            <div class="text-sm">Sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit.</div>
        </Tooltip> *@
    </header>
    <div class="px-5 py-3">
        <div class="flex items-start">
            <div class="text-3xl font-bold text-gray-800 dark:text-gray-100 mr-2">+$6,796</div>
            <div class="text-sm font-medium text-red-700 px-1.5 bg-red-500/20 rounded-full">-34%</div>
        </div>
    </div>
    <!-- Chart -->
    <div class="grow">
        @* TODO: Migrate to use BarChartCard component from AppBlueprint.UiKit.Components.Shared.Cruip *@
        @* <BarChartCard Height="248px" Labels="@labels" Datasets="@ConvertDatasets(datasets)" /> *@
        <div class="flex items-center justify-center h-32 text-gray-400">Chart placeholder - needs migration</div>
    </div>
</div>

@code {
    private List<string> labels = new()
    {
        "12-01-2022", "01-01-2023", "02-01-2023",
        "03-01-2023", "04-01-2023", "05-01-2023"
    };

    @* TODO: Migrate datasets when implementing charts\n    private List<MosaicPortal.Web.Components.Charts.BarChart.BarChartDataset> datasets = new()
    {
        new()
        {
            Label = "Stack 1",
            Data = new List<double> { 6200, 9200, 6600, 8800, 5200, 9200 },
            BackgroundColor = "rgb(139, 92, 246)",
            HoverBackgroundColor = "rgb(124, 58, 237)",
            BarPercentage = 0.7,
            CategoryPercentage = 0.7,
            BorderRadius = 4
        },
        new()
        {
            Label = "Stack 2",
            Data = new List<double> { -4000, -2600, -5350, -4000, -7500, -2000 },
            BackgroundColor = "rgb(221, 214, 254)",
            HoverBackgroundColor = "rgb(196, 181, 253)",
            BarPercentage = 0.7,
            CategoryPercentage = 0.7,
            BorderRadius = 4
        }
    };\n    *@
}